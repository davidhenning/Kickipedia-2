{% include 'header.twig' with {'title': 'Liste'} %}
{% import 'tools.twig' as tools %}

<header>
    <h2>&Uuml;bersicht</h2>
    <nav class="types">
        {% spaceless %}
            <ul>
                {% for type in types %}
                    <li {% if type.active == true %}class="active"{% endif %}>
                        <a href="{{ type.url }}">{{ type.name }}</a>
                    </li>
                {% endfor %}
            </ul>
        {% endspaceless %}
    </nav>
</header>
<div id="content">
    {{ tools.paginationSnippet(pagination, 'top') }}

    <table id="entryList" data-module="modules/entry/list">
        <tr>
            <th class="tools"></th>
            <th class="date">Datum:</th>
            <th class="name">Name:</th>
            <th class="ip">IP:</th>
            <th class="reason">Grund:</th>
        </tr>
        {% for entry in entries %}
            <tr>
                <td>
                    {% spaceless %}
                        <ul class="tools">
                            <li><a class="edit" href="/entry/{{ entry._id }}/edit">Bearbeiten</a></li>
                            <li><a class="delete" href="/entry/{{ entry._id }}/delete">L&ouml;schen</a></li>
                        </ul>
                    {% endspaceless %}
                </td>
                <td>{{ entry.createdOn|date("d.m.Y") }}</td>
                <td><a href="/entry/{{ entry._id }}/edit">{{ entry.name }}</a></td>
                <td>{{ entry.ip }}</td>
                <td>{{ entry.reason }}</td>
            </tr>
        {% endfor %}
    </table>

    {{ tools.paginationSnippet(pagination, 'bottom') }}
</div>

{% include 'footer.twig' %}
